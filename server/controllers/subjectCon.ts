import UserStudent from '../model/UserStudent';
import SubjectStudent from '../model/SubjectModel';

export async function saveGrade(req , res ){

    const newSubjects = {}
    // I think we need to take it from the cookies (user.id )
    // const newGradeDB = new SubjectStudent ({...newSubjects , ownerId:user._id})
}


